<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ECommerceApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_image_placeholder"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_image_placeholder"
        android:supportsRtl="true"
        android:theme="@style/Theme.ECommerceApp"
        android:usesCleartextTraffic="true">

        <!-- Main entry point — decides between Login and Home based on token -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
